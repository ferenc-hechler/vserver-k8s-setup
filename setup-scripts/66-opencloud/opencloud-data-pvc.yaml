apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: opencloud-data-pvc
  namespace: opencloud
  labels:
    app: opencloud
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
