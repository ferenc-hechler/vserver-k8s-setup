apiVersion: v1
kind: Service
metadata:
  name: testapp2-service
  labels:
    app: testapp2
spec:
  type: LoadBalancer
  selector:
    app: testapp2
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
